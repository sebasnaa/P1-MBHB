from _operator import or_

import  numpy as np
import itertools
import random

from random import randrange

#
#
# T =  T0
# S_act = generar_solucion_actual
#
# while T >= Tf:
#     for contador in range(200): # condicion de enfriamiento L(T)
#         s_cand = genera_vecino(s_act)
#         diff = coste(s_cand) - coste(s_act)
#         if (U(0,1) < e(-diff/T)) or diff < 0:
#             s_act = s_cand
#     T = alpha*T
#
#





print(r)